
query PRQuery($repo_name: String!, $pr_number: Int!){
    repository(owner: "AlmogZinger", name: $repo_name){
        pullRequest(number: $pr_number){
            ...PRMessage
        }
    }
}
fragment PRMessage on Comment {
  author {
    avatarUrl
  }
  bodyText
}
